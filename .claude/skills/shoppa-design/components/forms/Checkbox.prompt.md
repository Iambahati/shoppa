Square checkbox with inline label; fills emerald when checked. Use for opt-ins, filters and consent.

```jsx
<Checkbox name="remember" label="Keep me signed in" defaultChecked />
<Checkbox name="instock" label="In stock only" />
```

Give every checkbox a unique `name` (it scopes the checked-state CSS).
