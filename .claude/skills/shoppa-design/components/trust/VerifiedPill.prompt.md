The Shoppa trust mark — an emerald capsule with the filled check-badge glyph reading "Shoppa Verified". The single most important brand signal; place it on the auth brand-lock, verified device rows, dashboard trust callouts.

```jsx
<VerifiedPill />
<VerifiedPill size="sm" />
<VerifiedPill size="lg">Verified seller</VerifiedPill>
```

Sizes `sm | md | lg`. Pass children to relabel (default is "Shoppa Verified"). Never recolour it — emerald is load-bearing here.
