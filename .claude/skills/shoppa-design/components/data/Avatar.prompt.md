Circular initials avatar (optional image). Solid emerald = signed-in user; soft tint = list/table rows.

```jsx
<Avatar name="Jane Wanjiru" />
<Avatar name="Otieno K" tone="soft" size="sm" />
<Avatar name="Admin" size="lg" src="/uploads/me.jpg" />
```

Sizes `sm | md | lg`; tones `solid | soft`.
